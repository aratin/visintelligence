extends ../layouts/default
block content
    section
      #inner-banner
      #inner-content-box
        #content-box
          .container
            #offerings
              include ../views/icon.jade
              .col-md-9#content-data
                h2 #{data.ecommerceportals[0].heading}
                .row
                  .col-md-4
                    if data.ecommerceportals.length
                        if data.ecommerceportals[0].image.exists
                            img(id="image",src=data.ecommerceportals[0].image.url)
                  .col-md-8
                      p
                        | #{data.ecommerceportals[0].text}
                        br
                      p
                        | #{data.ecommerceportals[0].subText}
                        br
                      p
                        | #{data.ecommerceportals[0].shortString}
                        br
                      p
                        | #{data.ecommerceportals[0].longString}
              .expand
              include ../views/listingmenu
            